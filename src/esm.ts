export interface ESMOptions {
  /**
   * Return a pinned url
   */
  pin?: boolean;
  min?: boolean;
}

export async function resolveESM(module: string, options?: ESMOptions) {
  try {
    
  } catch (e) {
    console.error(e);
    return undefined;
  }
}
